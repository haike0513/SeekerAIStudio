# SeekerAIStudio UI/UX 设计系统

> 版本: 2.0
> 最后更新: 2026-01-16
> 设计语言: Midnight Glow

---

## 目录
1. [设计理念](#1-设计理念)
2. [色彩系统](#2-色彩系统)
3. [排版系统](#3-排版系统)
4. [间距系统](#4-间距系统)
5. [组件库](#5-组件库)
6. [布局系统](#6-布局系统)
7. [动效规范](#7-动效规范)
8. [交互模式](#8-交互模式)
9. [无障碍设计](#9-无障碍设计)
10. [模块专属设计](#10-模块专属设计)

---

## 1. 设计理念

### 1.1 设计语言: Midnight Glow

我们的设计语言名为 **"Midnight Glow"**（午夜微光），灵感来源于深夜创作者屏幕前的专注时刻。

**核心特征:**
- 深邃的背景，如同无边的画布
- 柔和的发光效果，引导视觉焦点
- 精致的玻璃拟态，增添层次与深度
- 流畅的微动效，让界面充满生命力

### 1.2 设计原则

#### 1.2.1 创作者优先 (Creator First)
> 界面应该退居幕后，让创作内容成为绝对主角。

- 极简的 Chrome（界面框架）
- 大面积的内容展示区
- 可隐藏的工具面板
- 沉浸式全屏模式

#### 1.2.2 认知负荷最小化 (Cognitive Load Minimization)
> 用户应该专注于创作，而非学习软件。

- 渐进式披露：先展示核心功能，高级功能按需呈现
- 一致的交互模式：相似操作使用相同手势
- 清晰的视觉层级：重要信息在视觉上突出
- 智能默认值：合理的预设减少决策负担

#### 1.2.3 反馈即时性 (Immediate Feedback)
> 每个操作都应该有即时、明确的反馈。

- 悬停状态变化 (< 50ms)
- 点击涟漪效果 (< 100ms)
- 加载进度指示 (任何 > 300ms 操作)
- 成功/失败 Toast 提示

#### 1.2.4 高效与美观并重 (Efficiency & Aesthetics)
> 美丽的设计不应牺牲效率，高效的设计不应牺牲美感。

- 快捷键覆盖所有高频操作
- 命令面板 (`Cmd+K`) 快速访问任何功能
- 动效时长经过精确调校，优雅但不拖沓
- 每个像素都经过精心设计

### 1.3 品牌调性

| 维度 | 描述 | 关键词 |
|------|------|--------|
| **专业性** | 面向严肃创作者的工具 | 精准、可靠、强大 |
| **现代感** | 采用最前沿的设计趋势 | 简约、科技、未来 |
| **温度感** | 不只是冰冷的工具 | 友好、陪伴、激励 |
| **创造力** | 激发用户的创作灵感 | 自由、开放、无限 |

---

## 2. 色彩系统

### 2.1 色彩定义

#### 2.1.1 基础色板 (Base Palette)

**背景层 (Background)**
```css
--bg-base:       hsl(222, 47%, 6%);    /* #0a0d14 - 最深背景 */
--bg-surface:    hsl(222, 41%, 9%);    /* #0f1319 - 卡片/面板 */
--bg-elevated:   hsl(222, 38%, 12%);   /* #151a24 - 悬浮层 */
--bg-overlay:    hsl(222, 35%, 15%);   /* #1c222f - 模态框 */
```

**前景层 (Foreground)**
```css
--text-primary:    hsl(210, 40%, 98%);   /* #f9fafb - 主要文本 */
--text-secondary:  hsl(215, 20%, 65%);   /* #9ca3af - 次要文本 */
--text-muted:      hsl(215, 16%, 47%);   /* #6b7280 - 禁用/辅助 */
--text-inverse:    hsl(222, 47%, 11%);   /* #111827 - 反色文本 */
```

**边框 (Border)**
```css
--border-subtle:   hsl(215, 20%, 18%);   /* #262d38 - 微妙分隔 */
--border-default:  hsl(215, 18%, 24%);   /* #374151 - 默认边框 */
--border-strong:   hsl(215, 16%, 35%);   /* #4b5563 - 强调边框 */
```

#### 2.1.2 主题色 (Accent Colors)

**主色调 - 电光紫 (Electric Violet)**
```css
--accent-50:   hsl(262, 83%, 95%);   /* #f0e7ff */
--accent-100:  hsl(262, 83%, 90%);   /* #e0cfff */
--accent-200:  hsl(262, 83%, 80%);   /* #c29fff */
--accent-300:  hsl(262, 83%, 70%);   /* #a370ff */
--accent-400:  hsl(262, 83%, 60%);   /* #8540ff */
--accent-500:  hsl(262, 83%, 58%);   /* #7c3aed - 主色 ★ */
--accent-600:  hsl(262, 72%, 51%);   /* #6d28d9 */
--accent-700:  hsl(263, 70%, 42%);   /* #5b21b6 */
--accent-800:  hsl(263, 69%, 32%);   /* #4c1d95 */
--accent-900:  hsl(264, 67%, 25%);   /* #3b0f7d */
```

**辅助色调 (Secondary Accents)**

用于区分不同功能模块：
```css
/* 小说模块 - 古典紫罗兰 */
--novel-accent:    hsl(280, 65%, 58%);   /* #a855f7 */

/* 漫画模块 - 电光青 */
--comic-accent:    hsl(188, 94%, 50%);   /* #06b6d4 */

/* 音频模块 - 霓虹玫红 */
--audio-accent:    hsl(330, 81%, 60%);   /* #ec4899 */

/* 视频模块 - 琥珀橙 */
--video-accent:    hsl(38, 92%, 50%);    /* #f59e0b */
```

#### 2.1.3 语义色 (Semantic Colors)

```css
/* 成功 */
--success-bg:      hsl(142, 76%, 12%);   /* 背景 */
--success-border:  hsl(142, 71%, 35%);   /* 边框 */
--success-text:    hsl(142, 71%, 45%);   /* 文本 */
--success-solid:   hsl(142, 71%, 45%);   /* 实心 #22c55e */

/* 警告 */
--warning-bg:      hsl(45, 93%, 12%);
--warning-border:  hsl(45, 93%, 40%);
--warning-text:    hsl(45, 93%, 47%);
--warning-solid:   hsl(45, 93%, 47%);    /* #eab308 */

/* 错误 */
--error-bg:        hsl(0, 72%, 12%);
--error-border:    hsl(0, 72%, 45%);
--error-text:      hsl(0, 72%, 55%);
--error-solid:     hsl(0, 72%, 51%);     /* #ef4444 */

/* 信息 */
--info-bg:         hsl(199, 89%, 12%);
--info-border:     hsl(199, 89%, 45%);
--info-text:       hsl(199, 89%, 55%);
--info-solid:      hsl(199, 89%, 48%);   /* #0ea5e9 */
```

### 2.2 色彩应用规范

#### 2.2.1 背景层级
```
┌─────────────────────────────────────────────────────────────┐
│  App 背景 (--bg-base)                                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  侧边栏 (--bg-surface)                                 │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  卡片 (--bg-elevated)                            │  │  │
│  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │  悬浮菜单 (--bg-overlay)                   │  │  │  │
│  │  │  └───────────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

#### 2.2.2 文本色彩应用
| 用途 | 色彩变量 | 示例 |
|------|---------|------|
| 标题/重要内容 | `--text-primary` | 页面标题、按钮文字 |
| 正文/说明 | `--text-secondary` | 段落文本、输入提示 |
| 辅助/禁用 | `--text-muted` | 时间戳、禁用按钮 |
| 链接/交互 | `--accent-500` | 可点击文本 |

#### 2.2.3 玻璃拟态 (Glassmorphism)
```css
.glass-panel {
  background: rgba(15, 19, 25, 0.8);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.05);
  box-shadow: 
    0 4px 6px -1px rgba(0, 0, 0, 0.1),
    0 2px 4px -2px rgba(0, 0, 0, 0.1),
    inset 0 1px 0 0 rgba(255, 255, 255, 0.05);
}
```

### 2.3 暗色/亮色模式

**默认**: 深色模式 (Dark Mode)

**可选**: 亮色模式 (Light Mode) - 规划中

**护眼模式**: 羊皮纸色 (Sepia) - 专用于小说模块

---

## 3. 排版系统

### 3.1 字体家族

```css
/* 界面字体 - 适用于 UI 元素 */
--font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', 
             Roboto, 'Helvetica Neue', sans-serif;

/* 等宽字体 - 适用于代码和数值 */
--font-mono: 'JetBrains Mono', 'Fira Code', 'Cascadia Code', 
             'Source Code Pro', monospace;

/* 正文字体 - 适用于小说阅读 */
--font-serif: 'Noto Serif SC', 'Source Han Serif SC', 
              'Songti SC', serif;
```

### 3.2 字号比例 (Type Scale)

基于 1.25 比例 (Major Third)：

```css
--text-xs:    0.75rem;    /* 12px */
--text-sm:    0.875rem;   /* 14px */
--text-base:  1rem;       /* 16px - 基准 */
--text-lg:    1.125rem;   /* 18px */
--text-xl:    1.25rem;    /* 20px */
--text-2xl:   1.5rem;     /* 24px */
--text-3xl:   1.875rem;   /* 30px */
--text-4xl:   2.25rem;    /* 36px */
--text-5xl:   3rem;       /* 48px */
```

### 3.3 行高 (Line Height)

```css
--leading-none:    1;
--leading-tight:   1.25;
--leading-snug:    1.375;
--leading-normal:  1.5;    /* 默认 */
--leading-relaxed: 1.625;
--leading-loose:   2;
```

### 3.4 字重 (Font Weight)

```css
--font-normal:   400;
--font-medium:   500;
--font-semibold: 600;
--font-bold:     700;
```

### 3.5 排版应用

#### 标题层级
| 级别 | 字号 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| H1 | 2.25rem (36px) | 700 | 1.25 | 页面标题 |
| H2 | 1.875rem (30px) | 600 | 1.25 | 区块标题 |
| H3 | 1.5rem (24px) | 600 | 1.375 | 卡片标题 |
| H4 | 1.25rem (20px) | 600 | 1.375 | 小节标题 |
| H5 | 1.125rem (18px) | 500 | 1.5 | 辅助标题 |
| Body | 1rem (16px) | 400 | 1.5 | 正文 |
| Small | 0.875rem (14px) | 400 | 1.5 | 辅助文字 |
| Tiny | 0.75rem (12px) | 400 | 1.5 | 标签/时间 |

---

## 4. 间距系统

### 4.1 间距比例

基于 4px 基准单位：

```css
--space-0:   0;
--space-0.5: 0.125rem;  /* 2px */
--space-1:   0.25rem;   /* 4px */
--space-1.5: 0.375rem;  /* 6px */
--space-2:   0.5rem;    /* 8px */
--space-2.5: 0.625rem;  /* 10px */
--space-3:   0.75rem;   /* 12px */
--space-3.5: 0.875rem;  /* 14px */
--space-4:   1rem;      /* 16px */
--space-5:   1.25rem;   /* 20px */
--space-6:   1.5rem;    /* 24px */
--space-7:   1.75rem;   /* 28px */
--space-8:   2rem;      /* 32px */
--space-9:   2.25rem;   /* 36px */
--space-10:  2.5rem;    /* 40px */
--space-12:  3rem;      /* 48px */
--space-14:  3.5rem;    /* 56px */
--space-16:  4rem;      /* 64px */
--space-20:  5rem;      /* 80px */
--space-24:  6rem;      /* 96px */
```

### 4.2 间距应用

| 场景 | 间距 | 说明 |
|------|------|------|
| 组件内边距 (小) | 8px (--space-2) | 按钮、输入框 |
| 组件内边距 (中) | 12px (--space-3) | 卡片、面板 |
| 组件内边距 (大) | 16px-24px | 大型容器 |
| 元素间隙 (紧凑) | 4px (--space-1) | 图标与文字 |
| 元素间隙 (默认) | 8px (--space-2) | 表单元素 |
| 元素间隙 (宽松) | 16px (--space-4) | 卡片之间 |
| 区块分隔 | 24px-32px | 页面区块 |

### 4.3 圆角系统

```css
--radius-none: 0;
--radius-sm:   0.125rem;  /* 2px */
--radius-md:   0.375rem;  /* 6px */
--radius-lg:   0.5rem;    /* 8px */
--radius-xl:   0.75rem;   /* 12px */
--radius-2xl:  1rem;      /* 16px */
--radius-3xl:  1.5rem;    /* 24px */
--radius-full: 9999px;    /* 圆形 */
```

### 4.4 阴影系统

```css
--shadow-sm:    0 1px 2px 0 rgba(0, 0, 0, 0.05);
--shadow-md:    0 4px 6px -1px rgba(0, 0, 0, 0.1),
                0 2px 4px -2px rgba(0, 0, 0, 0.1);
--shadow-lg:    0 10px 15px -3px rgba(0, 0, 0, 0.1),
                0 4px 6px -4px rgba(0, 0, 0, 0.1);
--shadow-xl:    0 20px 25px -5px rgba(0, 0, 0, 0.1),
                0 8px 10px -6px rgba(0, 0, 0, 0.1);
--shadow-2xl:   0 25px 50px -12px rgba(0, 0, 0, 0.25);
--shadow-inner: inset 0 2px 4px 0 rgba(0, 0, 0, 0.05);

/* 发光效果 */
--glow-accent:  0 0 20px rgba(124, 58, 237, 0.25);
--glow-success: 0 0 20px rgba(34, 197, 94, 0.25);
--glow-error:   0 0 20px rgba(239, 68, 68, 0.25);
```

---

## 5. 组件库

### 5.1 按钮 (Button)

#### 变体
```tsx
// 主要按钮 - 用于主操作
<Button variant="default">生成</Button>

// 次要按钮 - 用于次要操作
<Button variant="secondary">取消</Button>

// 轮廓按钮 - 用于可选操作
<Button variant="outline">导出</Button>

// 幽灵按钮 - 用于工具栏
<Button variant="ghost">设置</Button>

// 危险按钮 - 用于破坏性操作
<Button variant="destructive">删除</Button>

// 链接按钮 - 用于导航
<Button variant="link">了解更多</Button>
```

#### 尺寸
```tsx
<Button size="sm">小</Button>   // 高度 32px
<Button size="default">默认</Button>  // 高度 40px
<Button size="lg">大</Button>   // 高度 48px
<Button size="icon">🔧</Button> // 40x40px 正方形
```

#### 状态
- **默认**: 正常显示
- **悬停**: 亮度提升 + 轻微上浮
- **按下**: 亮度降低 + 轻微下沉
- **聚焦**: 显示焦点环
- **禁用**: 降低透明度 + 禁止光标

### 5.2 输入框 (Input)

```tsx
<Input 
  placeholder="请输入..."
  disabled={false}
  invalid={hasError}
/>
```

**样式规范:**
- 高度: 40px
- 圆角: 8px
- 边框: 1px solid var(--border-default)
- 焦点: 主题色边框 + 发光效果
- 错误: 红色边框 + 红色发光

### 5.3 对话框 (Dialog)

```tsx
<Dialog>
  <DialogTrigger>打开</DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>标题</DialogTitle>
      <DialogDescription>描述文字</DialogDescription>
    </DialogHeader>
    <DialogBody>内容区域</DialogBody>
    <DialogFooter>
      <Button variant="outline">取消</Button>
      <Button>确认</Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

**样式规范:**
- 背景: var(--bg-overlay) + 背景模糊
- 圆角: 16px
- 最大宽度: 500px (可配置)
- 遮罩: 半透明黑色
- 动画: 淡入 + 缩放

### 5.4 下拉菜单 (Dropdown Menu)

```tsx
<DropdownMenu>
  <DropdownMenuTrigger>选项</DropdownMenuTrigger>
  <DropdownMenuContent>
    <DropdownMenuItem>编辑</DropdownMenuItem>
    <DropdownMenuItem>复制</DropdownMenuItem>
    <DropdownMenuSeparator />
    <DropdownMenuItem variant="destructive">删除</DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```

### 5.5 Toast 提示

```tsx
// 成功提示
toast.success("保存成功");

// 错误提示
toast.error("保存失败");

// 信息提示
toast.info("正在处理...");

// 警告提示
toast.warning("请注意...");
```

**位置**: 右下角
**持续时间**: 3 秒 (可配置)
**动画**: 从右侧滑入/滑出

### 5.6 命令面板 (Command Palette)

快捷键: `Cmd+K` / `Ctrl+K`

```tsx
<CommandDialog>
  <CommandInput placeholder="搜索命令..." />
  <CommandList>
    <CommandEmpty>未找到结果</CommandEmpty>
    <CommandGroup heading="快捷操作">
      <CommandItem>新建对话</CommandItem>
      <CommandItem>打开设置</CommandItem>
    </CommandGroup>
    <CommandGroup heading="导航">
      <CommandItem>小说工作台</CommandItem>
      <CommandItem>漫画工作台</CommandItem>
    </CommandGroup>
  </CommandList>
</CommandDialog>
```

---

## 6. 布局系统

### 6.1 全局布局

```
┌────────────────────────────────────────────────────────────────┐
│                        Title Bar (32px)                        │
├──────────┬─────────────────────────────────────────────────────┤
│          │                                                     │
│          │                                                     │
│  Sidebar │                   Main Content                      │
│  (240px) │                                                     │
│   可收缩  │                                                     │
│          │                                                     │
│          │                                                     │
│          ├─────────────────────────────────────────────────────┤
│          │                Status Bar (24px)                    │
└──────────┴─────────────────────────────────────────────────────┘
```

### 6.2 响应式断点

```css
--breakpoint-sm:  640px;   /* 手机横屏 */
--breakpoint-md:  768px;   /* 平板竖屏 */
--breakpoint-lg:  1024px;  /* 平板横屏/小屏笔记本 */
--breakpoint-xl:  1280px;  /* 桌面显示器 */
--breakpoint-2xl: 1536px;  /* 大屏显示器 */
```

### 6.3 侧边栏

**默认宽度**: 240px
**收缩宽度**: 64px
**最小化**: 完全隐藏

**结构:**
```
┌──────────────────────────┐
│     Logo & App Name      │
├──────────────────────────┤
│     Navigation Items     │
│  📝 对话                  │
│  📖 小说                  │
│  🎨 漫画                  │
│  🎵 音频                  │
│  🎬 视频                  │
│  ⚡ 工作流                │
├──────────────────────────┤
│     Quick Actions        │
├──────────────────────────┤
│  ⚙️ 设置    👤 用户       │
└──────────────────────────┘
```

### 6.4 三栏布局 (创作工具专用)

```
┌─────────┬───────────────────────────────────────────┬─────────┐
│         │                                           │         │
│  Left   │              Center                       │  Right  │
│  Panel  │              Canvas                       │  Panel  │
│ (240px) │              (flex)                       │ (300px) │
│         │                                           │         │
│ 大纲/    │              内容编辑区                    │ 属性/   │
│ 资源     │                                           │ 生成    │
│         │                                           │         │
└─────────┴───────────────────────────────────────────┴─────────┘
```

---

## 7. 动效规范

### 7.1 时长标准

```css
--duration-instant:  0ms;     /* 即时 */
--duration-fast:     100ms;   /* 快速 - 悬停/焦点 */
--duration-normal:   200ms;   /* 正常 - 展开/收起 */
--duration-slow:     300ms;   /* 缓慢 - 页面过渡 */
--duration-slower:   500ms;   /* 更慢 - 复杂动画 */
```

### 7.2 缓动函数

```css
/* 标准缓动 */
--ease-in:       cubic-bezier(0.4, 0, 1, 1);
--ease-out:      cubic-bezier(0, 0, 0.2, 1);
--ease-in-out:   cubic-bezier(0.4, 0, 0.2, 1);

/* 特殊缓动 */
--ease-bounce:   cubic-bezier(0.34, 1.56, 0.64, 1);
--ease-spring:   cubic-bezier(0.175, 0.885, 0.32, 1.275);
--ease-smooth:   cubic-bezier(0.45, 0.05, 0.55, 0.95);
```

### 7.3 常用动效

#### 淡入淡出
```css
.fade-enter {
  opacity: 0;
}
.fade-enter-active {
  opacity: 1;
  transition: opacity 200ms ease-out;
}
.fade-exit {
  opacity: 1;
}
.fade-exit-active {
  opacity: 0;
  transition: opacity 150ms ease-in;
}
```

#### 缩放弹出
```css
.scale-enter {
  opacity: 0;
  transform: scale(0.95);
}
.scale-enter-active {
  opacity: 1;
  transform: scale(1);
  transition: all 200ms ease-spring;
}
```

#### 滑入滑出
```css
.slide-right-enter {
  transform: translateX(100%);
}
.slide-right-enter-active {
  transform: translateX(0);
  transition: transform 300ms ease-out;
}
```

### 7.4 微交互

#### 按钮悬停
```css
.button:hover {
  transform: translateY(-1px);
  box-shadow: var(--shadow-md);
  transition: all 100ms ease-out;
}

.button:active {
  transform: translateY(0);
  box-shadow: var(--shadow-sm);
}
```

#### 加载旋转
```css
@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.spinner {
  animation: spin 1s linear infinite;
}
```

#### 脉冲效果
```css
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.loading-dot {
  animation: pulse 2s ease-in-out infinite;
}
```

#### 打字机效果
```css
@keyframes typing {
  from { max-width: 0; }
  to { max-width: 100%; }
}

.typewriter {
  overflow: hidden;
  white-space: nowrap;
  animation: typing 2s steps(40) forwards;
}
```

---

## 8. 交互模式

### 8.1 键盘导航

#### 全局快捷键
| 快捷键 | 操作 |
|--------|------|
| `Cmd+K` | 打开命令面板 |
| `Cmd+,` | 打开设置 |
| `Cmd+N` | 新建对话/项目 |
| `Cmd+S` | 保存 |
| `Cmd+Z` | 撤销 |
| `Cmd+Shift+Z` | 重做 |
| `Escape` | 关闭弹窗/取消操作 |

#### 编辑器快捷键
| 快捷键 | 操作 |
|--------|------|
| `Tab` | 接受 AI 建议 |
| `Escape` | 取消 AI 建议 |
| `/` | 打开 Slash 命令菜单 |
| `@` | 引用角色/设定 |
| `Cmd+Enter` | 发送/生成 |

### 8.2 拖放交互

#### 可拖放元素
- 侧边栏章节项 (调整顺序)
- 时间轴片段 (调整位置)
- 画布上的分镜 (自由移动)
- 资源面板文件 (添加到画布)

#### 拖放反馈
- 开始拖动: 提升阴影，轻微缩放
- 拖动中: 拖影跟随光标
- 可放置区: 边框高亮
- 放置: 动画过渡到目标位置

### 8.3 右键菜单

**通用菜单项:**
- 编辑
- 复制
- 粘贴
- 删除
- (分隔线)
- 属性

### 8.4 文本选择操作

选中文本后显示浮动工具条:
```
┌─────────────────────────────────────┐
│  润色 │ 扩写 │ 缩写 │ 翻译 │ 复制  │
└─────────────────────────────────────┘
```

---

## 9. 无障碍设计

### 9.1 色彩对比度

所有文本必须满足 WCAG 2.1 AA 标准:
- 正常文本: 对比度 ≥ 4.5:1
- 大文本 (18px+): 对比度 ≥ 3:1

### 9.2 键盘可访问性

- 所有交互元素可通过 Tab 键聚焦
- 聚焦元素有清晰的视觉指示 (焦点环)
- 支持 Enter/Space 激活元素
- 支持 Escape 取消/关闭

### 9.3 屏幕阅读器支持

- 所有图标有 `aria-label`
- 表单元素有 `label` 关联
- 动态内容使用 `aria-live` 区域
- 正确使用语义化 HTML 标签

### 9.4 运动敏感

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 10. 模块专属设计

### 10.1 小说模块

**主题色**: `--novel-accent` (紫罗兰)

**特殊 UI 元素:**
- **编辑器**: 适合长时间阅读的字体和行高
- **大纲树**: 可展开/折叠的章节结构
- **角色卡**: 包含头像、属性的卡片
- **Ghost Text**: 灰色半透明的 AI 建议文本

**护眼模式:**
```css
.sepia-mode {
  --bg-base: hsl(36, 43%, 95%);
  --text-primary: hsl(36, 10%, 20%);
  filter: sepia(10%);
}
```

### 10.2 漫画模块

**主题色**: `--comic-accent` (电光青)

**特殊 UI 元素:**
- **无限画布**: 支持平移缩放的画布
- **分镜格子**: 可调整大小和位置
- **气泡编辑器**: 支持多种气泡样式
- **图层面板**: 类 Photoshop 的图层列表

### 10.3 音频模块

**主题色**: `--audio-accent` (霓虹玫红)

**特殊 UI 元素:**
- **多轨时间轴**: 水平滚动的波形轨道
- **波形显示**: 高饱和度的波形可视化
- **混音推子**: 音量/声像控制
- **频谱分析**: 实时频率显示

### 10.4 视频模块

**主题色**: `--video-accent` (琥珀橙)

**特殊 UI 元素:**
- **视频时间轴**: 带缩略图的视频轨
- **播放器**: 嵌入式视频预览
- **关键帧编辑器**: 动画曲线编辑
- **导出进度**: 渲染进度条

---

## 附录

### A. 设计资源

- **Figma 组件库**: (链接待添加)
- **图标库**: Lucide Icons
- **字体**: Google Fonts - Inter, JetBrains Mono

### B. 相关文档

- [产品设计文档](../product/PRD.md)
- [技术架构文档](../technical/Architecture.md)
- [小说模块 UI](./AIGC_Novel_UI.md)
- [漫画模块 UI](./AIGC_Comic_UI.md)
- [音频模块 UI](./AIGC_Audio_UI.md)
- [视频模块 UI](./AIGC_Video_UI.md)

### C. 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-01-16 | 2.0 | 完整的设计系统规范 |
| 2026-01-01 | 1.0 | 初始版本 |
